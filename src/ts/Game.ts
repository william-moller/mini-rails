/**
 *------
 * BGA framework: Gregory Isabelli & Emmanuel Colin & BoardGameArena
 * Mini Rails implementation : © Will Moller <will.moller@gmail.com>
 *
 * This code has been produced on the BGA studio platform for use on http://boardgamearena.com.
 * See http://en.boardgamearena.com/#!doc/Studio for more information.
 * -----
 */

/*
 * PLACEHOLDER — this file exists only so `npm run build:ts` has an entry point and the toolchain can
 * be verified before there is any game code.
 *
 * Replace it with the client from BGA's generated project skeleton (pull the skeleton over SFTP and
 * commit it first — see ../../.claude/deploy.md, "Starting a new game"). Do not hand-author the
 * client, the PHP state classes, or Game.php from scratch; start from what BGA generated and edit
 * from there.
 *
 * ⚠️ Never put a backtick inside a template literal in this tree, not even inside a comment — it
 * terminates the literal, and rollup's TypeScript plugin reports the resulting parse errors as
 * *warnings*, still emits Game.js, and exits 0. See ../../.claude/deploy.md.
 */

export {};
